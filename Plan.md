# 新的开发任务：

重新思考 IVoiceCommand 和 CommandEngine 的可能抽象：

现象：

1. “打击然后打击” 之前会尝试两次放入 1号打击， 通过出牌时强制刷新词表解决了
2. “打击4然后打击5” 之前正常，但是现在由于修改1，抽掉打击4之后就找不到打击5了
3. Execute(string word) 相当地字符串编程，非常不便于排查错误
4. 在 GetVocab 往往已经成功获取可以执行的物品， word 还要重复获取一次
5. 具体检查卡牌是否能打出 / 能否选择附加按钮 很可能需要 类内环境，上交闭包 太重 （这点有待商榷）
6. 现在 “防御双重释放然后打击” 等 误识别的情况，以及 “ 出击(需要召唤物存活的情况) ” 以及上面找不到牌， 会导致部分卡牌成功打出，破坏一些需要的顺序
“这个命令恰好不归我管了”(静默拒绝)、 “这里有问题” (显著拒绝)、 “找不到命令” 等情况需要命令引擎做出处理？